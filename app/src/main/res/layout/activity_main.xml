<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#020617">

    <!-- Main Clean Dashboard Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top Native Toolbar -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/top_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#0f172a"
            app:navigationIcon="@android:drawable/ic_menu_sort_by_size"
            app:title="MINEGUARD"
            app:titleTextColor="#38bdf8"
            app:subtitle="DGMS Underground Console"
            app:subtitleTextColor="#94a3b8">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:orientation="horizontal"
                android:paddingEnd="12dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_lang_toggle"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="38dp"
                    android:text="🌐 English"
                    android:textColor="#38bdf8"
                    android:textSize="11sp"
                    app:strokeColor="#38bdf8" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_torch_toggle"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="wrap_content"
                    android:layout_height="38dp"
                    android:layout_marginStart="6dp"
                    android:text="🔦 Torch"
                    android:textSize="11sp" />
            </LinearLayout>
        </com.google.android.material.appbar.MaterialToolbar>

        <!-- Live Shift & Telemetry Strip -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:background="#020617"
            android:gravity="center_vertical"
            android:paddingHorizontal="14dp">

            <TextView
                android:id="@+id/tv_shift_hud"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="⏱️ SHIFT 1 • 06:00 - 14:00"
                android:textColor="#cbd5e1"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/tv_mesh_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🟢 MESH: ACTIVE"
                android:textColor="#34d399"
                android:textSize="11sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Centered Dashboard: ONLY THE EMERGENCY SIREN -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_emergency_siren"
                android:layout_width="300dp"
                android:layout_height="300dp"
                android:layout_gravity="center"
                android:clickable="true"
                android:focusable="true"
                app:cardBackgroundColor="#450a0a"
                app:cardCornerRadius="150dp"
                app:strokeColor="#ef4444"
                app:strokeWidth="4dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:orientation="vertical"
                    android:padding="20dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="🚨"
                        android:textSize="64sp" />

                    <TextView
                        android:id="@+id/tv_siren_title"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:text="EMERGENCY SIREN"
                        android:textColor="#f87171"
                        android:textSize="18sp"
                        android:textStyle="bold" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:gravity="center"
                        android:text="Broadcast Evacuation to All Phones"
                        android:textColor="#fca5a5"
                        android:textSize="11sp" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>
        </FrameLayout>
    </LinearLayout>

    <!-- Native Navigation 3-Pai Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#0f172a"
        app:itemIconTint="#38bdf8"
        app:itemTextColor="#f8fafc"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
